package com.practice.leetcode.codility;

import static org.junit.jupiter.api.Assertions.assertArrayEquals;
import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

/**
 * ╔══════════════════════════════════════════════════════════════════════════════╗
 * ║                           LESSON 2: ARRAYS                                  ║
 * ║                         Bài toán về mảng cơ bản                             ║
 * ╚══════════════════════════════════════════════════════════════════════════════╝
 * 
 * Lesson này tập trung vào các thao tác cơ bản với mảng.
 * Các bài toán thường gặp trong phỏng vấn để đánh giá hiểu biết về array.
 */
public class L2_Arrays {

    /**
     * ╔═══════════════════════════════════════════════════════════════════════════╗
     * ║                        CYCLIC ROTATION                                   ║
     * ║                    Xoay vòng mảng theo chu kỳ                            ║
     * ╚═══════════════════════════════════════════════════════════════════════════╝
     * 
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │ ĐỀ BÀI:                                                                    │
     * │ Cho một mảng A gồm N phần tử và số nguyên K.                               │
     * │ Xoay mảng sang phải K lần. Mỗi lần xoay, phần tử cuối chuyển lên đầu.     │
     * │                                                                             │
     * │ VÍ DỤ:                                                                      │
     * │ A = [3, 8, 9, 7, 6], K = 3                                                 │
     * │ Sau 1 lần xoay: [6, 3, 8, 9, 7]                                            │
     * │ Sau 2 lần xoay: [7, 6, 3, 8, 9]                                            │
     * │ Sau 3 lần xoay: [9, 7, 6, 3, 8]  ← Kết quả                                 │
     * └─────────────────────────────────────────────────────────────────────────────┘
     * 
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │ PHÂN TÍCH - TƯ DUY GIẢI QUYẾT:                                             │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │                                                                             │
     * │ 🎯 NHẬN XÉT QUAN TRỌNG:                                                    │
     * │ • Xoay N lần = mảng quay về vị trí ban đầu (N là độ dài mảng)             │
     * │ • Vì vậy chỉ cần xoay K % N lần là đủ                                      │
     * │ • Nếu K % N == 0, mảng không thay đổi                                      │
     * │                                                                             │
     * │ 💡 CÁCH 1: Sử dụng mảng phụ O(N)                                           │
     * │ • Tạo mảng mới, đặt mỗi phần tử vào vị trí mới: (i + K) % N                │
     * │                                                                             │
     * │ 💡 CÁCH 2: Reverse ba lần O(1) space                                       │
     * │ • Reverse toàn bộ mảng                                                      │
     * │ • Reverse K phần tử đầu                                                     │
     * │ • Reverse (N-K) phần tử còn lại                                            │
     * │                                                                             │
     * │ 📊 MINH HỌA CÁCH 2 với A = [3, 8, 9, 7, 6], K = 3:                         │
     * │ ┌─────────────────────────────────────────────────────────────────────────┐│
     * │ │ Ban đầu:        [3, 8, 9, 7, 6]                                        ││
     * │ │ Reverse all:    [6, 7, 9, 8, 3]                                        ││
     * │ │ Reverse [0,2]:  [9, 7, 6, 8, 3]                                        ││
     * │ │ Reverse [3,4]:  [9, 7, 6, 3, 8]  ← Kết quả                              ││
     * │ └─────────────────────────────────────────────────────────────────────────┘│
     * └─────────────────────────────────────────────────────────────────────────────┘
     * 
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │ ĐỘ PHỨC TẠP:                                                               │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │ Cách 1 (dùng mảng phụ): Thời gian O(N), Không gian O(N)                    │
     * │ Cách 2 (reverse): Thời gian O(N), Không gian O(1)                          │
     * └─────────────────────────────────────────────────────────────────────────────┘
     */
    public int[] cyclicRotation(int[] A, int K) {
        int N = A.length;
        
        // Edge cases
        if (N == 0 || K % N == 0) {
            return A;
        }
        
        // Chỉ cần xoay K % N lần
        K %= N;
        
        // Cách 1: Sử dụng mảng phụ
        int[] result = new int[N];
        for (int i = 0; i < N; i++) {
            result[(i + K) % N] = A[i];
        }
        
        return result;
    }

    /**
     * Cách 2: In-place rotation using reverse
     * Không dùng mảng phụ, tiết kiệm bộ nhớ
     */
    public int[] cyclicRotationInPlace(int[] A, int K) {
        int N = A.length;
        if (N == 0 || K % N == 0) return A;
        
        K = K % N;
        
        // Reverse toàn bộ
        reverse(A, 0, N - 1);
        // Reverse K phần tử đầu
        reverse(A, 0, K - 1);
        // Reverse phần còn lại
        reverse(A, K, N - 1);
        
        return A;
    }
    
    private void reverse(int[] A, int left, int right) {
        while (left < right) {
            int temp = A[left];
            A[left] = A[right];
            A[right] = temp;
            left++;
            right--;
        }
    }

    /**
     * ╔═══════════════════════════════════════════════════════════════════════════╗
     * ║                    ODD OCCURRENCES IN ARRAY                              ║
     * ║              Tìm phần tử xuất hiện lẻ lần trong mảng                     ║
     * ╚═══════════════════════════════════════════════════════════════════════════╝
     * 
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │ ĐỀ BÀI:                                                                    │
     * │ Cho mảng A có N phần tử (N lẻ). Mỗi phần tử xuất hiện chẵn lần,           │
     * │ trừ một phần tử xuất hiện lẻ lần. Tìm phần tử đó.                         │
     * │                                                                             │
     * │ VÍ DỤ:                                                                      │
     * │ A = [9, 3, 9, 3, 9, 7, 9]                                                  │
     * │ • 9 xuất hiện 4 lần (chẵn)                                                 │
     * │ • 3 xuất hiện 2 lần (chẵn)                                                 │
     * │ • 7 xuất hiện 1 lần (LẺ) ← Đáp án                                          │
     * └─────────────────────────────────────────────────────────────────────────────┘
     * 
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │ PHÂN TÍCH - TƯ DUY GIẢI QUYẾT:                                             │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │                                                                             │
     * │ 💡 Ý TƯỞNG: Sử dụng phép XOR (^)                                           │
     * │                                                                             │
     * │ 🔑 TÍNH CHẤT CỦA XOR:                                                      │
     * │ • a ^ a = 0      (XOR với chính nó = 0)                                    │
     * │ • a ^ 0 = a      (XOR với 0 = chính nó)                                    │
     * │ • a ^ b = b ^ a  (Giao hoán)                                               │
     * │ • (a^b)^c = a^(b^c)  (Kết hợp)                                             │
     * │                                                                             │
     * │ 📊 MINH HỌA:                                                               │
     * │ ┌─────────────────────────────────────────────────────────────────────────┐│
     * │ │ A = [9, 3, 9, 3, 9, 7, 9]                                               ││
     * │ │                                                                         ││
     * │ │ XOR tất cả:                                                             ││
     * │ │ 9 ^ 3 ^ 9 ^ 3 ^ 9 ^ 7 ^ 9                                               ││
     * │ │ = (9 ^ 9) ^ (9 ^ 9) ^ (3 ^ 3) ^ 7    (nhóm các cặp giống nhau)          ││
     * │ │ = 0 ^ 0 ^ 0 ^ 7                                                         ││
     * │ │ = 7  ← Đáp án                                                           ││
     * │ │                                                                         ││
     * │ │ → Các phần tử xuất hiện chẵn lần tự triệt tiêu nhau!                    ││
     * │ └─────────────────────────────────────────────────────────────────────────┘│
     * └─────────────────────────────────────────────────────────────────────────────┘
     * 
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │ ĐỘ PHỨC TẠP:                                                               │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │ • Thời gian: O(N) - duyệt mảng 1 lần                                       │
     * │ • Không gian: O(1) - chỉ cần 1 biến                                        │
     * └─────────────────────────────────────────────────────────────────────────────┘
     * 
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │ TẠI SAO XOR LÀ GIẢI PHÁP TUYỆT VỜI?                                        │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │ 1. Không cần bộ nhớ thêm (HashSet tốn O(N) space)                          │
     * │ 2. Chỉ duyệt 1 lần (sorting tốn O(N log N))                                │
     * │ 3. Hoạt động với số âm (vì XOR hoạt động trên bit)                         │
     * │ 4. Rất nhanh (phép toán bit nhanh hơn các phép toán khác)                  │
     * └─────────────────────────────────────────────────────────────────────────────┘
     */
    public int oddOccurrencesInArray(int[] A) {
        int result = 0;
        
        for (int num : A) {
            result ^= num;
        }
        
        return result;
    }
    
    // ================================ TESTS ================================
    
    @Test
    void testCyclicRotation_Example() {
        int[] A = {3, 8, 9, 7, 6};
        int[] expected = {9, 7, 6, 3, 8};
        assertArrayEquals(expected, cyclicRotation(A, 3));
    }
    
    @Test
    void testCyclicRotation_FullCycle() {
        int[] A = {1, 2, 3, 4, 5};
        int[] expected = {1, 2, 3, 4, 5};
        assertArrayEquals(expected, cyclicRotation(A, 5));
    }
    
    @Test
    void testCyclicRotation_EmptyArray() {
        int[] A = {};
        assertArrayEquals(new int[]{}, cyclicRotation(A, 3));
    }
    
    @Test
    void testCyclicRotation_SingleElement() {
        int[] A = {5};
        assertArrayEquals(new int[]{5}, cyclicRotation(A, 10));
    }
    
    @Test
    void testOddOccurrences_Example() {
        int[] A = {9, 3, 9, 3, 9, 7, 9};
        assertEquals(7, oddOccurrencesInArray(A));
    }
    
    @Test
    void testOddOccurrences_SingleElement() {
        int[] A = {42};
        assertEquals(42, oddOccurrencesInArray(A));
    }
    
    @Test
    void testOddOccurrences_AllSame() {
        int[] A = {5, 5, 5};
        assertEquals(5, oddOccurrencesInArray(A));
    }
}
